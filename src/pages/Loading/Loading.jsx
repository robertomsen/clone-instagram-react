import "./Loading.css";
import Spinner from "../../components/Spinner/Spinner";

const Loading = () => {
  return (
    <div id="loading--page">
      <Spinner />
    </div>
  );
};

export default Loading;
